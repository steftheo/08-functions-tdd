/**
 * Checks if two arrays have the same first value
 * @param  {Array} a
 * @param  {Array} b
 * @return {Boolean}
 */
sameFirst(a, b) {

}

/**
 * Checks if two arrays have the same values
 * @param  {Array} a
 * @param  {Array} b
 * @return {Boolean}
 */
deepEquals(a, b) {

}
